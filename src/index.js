import React from "react";
import ReactDOM from "react-dom";
import TextCounterApp from "./components/TextCounterApp";

ReactDOM.render(
  <React.StrictMode>
    <TextCounterApp />
  </React.StrictMode>,
  document.getElementById("root")
);
